# TODO

- [ ] Add a background layer to the hero section in `src/screens/Rootslanding.jsx` using `public/hero_light.png`.
- [ ] Style the new `.hero-bg` in `src/screens/Rootslanding.css` to sit behind content with a large negative 3D depth (`translateZ(-1000px)`).
- [x] Ensure the new background doesn’t capture pointer events and doesn’t affect layout.
- [x] Run the dev server/build to verify the background renders correctly behind hero content.


