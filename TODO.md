# TODO

- [x] Make login screen responsive
  - [x] Update `src/screens/Login.css` with mobile breakpoints (stack panels, reduce padding/font sizes)
  - [x] Fix overflow issue caused by `body { overflow: hidden; }` inside `Login.css`
  - [x] Ensure layout is scrollable on small screens (adjust min-height/margins)
  - [ ] Quick manual QA: resize to ~375px and ~768px widths; verify Login + MFA challenge


