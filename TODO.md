# TODO

- [ ] Wire SettingsPage theme UI to add/remove `html.dark` (source of truth).
- [ ] Persist theme choice in `localStorage` (e.g. `roots_theme`).
- [ ] On app load, apply persisted theme to `html.dark`.
- [ ] Make `src/components/Theme.jsx` functional OR remove it from affecting theme (keep as UI toggle only).
- [ ] Quick sanity test: open SettingsPage, toggle Dark/Light, verify CSS variables switch across pages.

